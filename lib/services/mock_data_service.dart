/// MockData is intentionally empty.
///
/// All market data comes from the Node.js backend (paper_trading_backend).
/// If the backend is unreachable, the app shows an error state — not fake data.
class MockData {
  MockData._();
}
